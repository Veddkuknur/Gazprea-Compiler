procedure main() returns integer {
    tuple(integer, integer[20]) rb_tup = (1, [1,2,3,4]);

    integer[*] c = false + true;
    return 0;
}

//CHECK:SizeError on Line 4: SizeError on Line 4: Cannot initialize a variable sized vector with a scalar
