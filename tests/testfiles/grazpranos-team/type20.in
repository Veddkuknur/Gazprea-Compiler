procedure main() returns integer {
    tuple(integer, integer[20]) rb_tup = (1, [1,2,3,4]);

    integer[*] c = 2^rb_tup.2 + [1, false];
    return 0;
}

//CHECK:TypeError on Line 4: Vector literal with members of incompatible types