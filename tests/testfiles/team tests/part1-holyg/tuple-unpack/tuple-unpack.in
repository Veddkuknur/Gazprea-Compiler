procedure main() returns integer {
    real x = 0;
    integer y = 0;
    tuple(real, integer) t = (6.1, 3);
    x, y = t;
    x -> std_output;
    y -> std_output;
    return 0;
}

// CHECK_FILE:test.out