typedef integer int;

procedure lol() returns int {

    return 1;
}
typedef boolean bool;
const bool a = true;

procedure main() returns integer {
    if (lol()){
    }
    return 0;
}

//CHECK_FILE:procCallInIfTypeError.out